name: Publish

permissions:
  contents: write
  pull-requests: write

on:
  # Trigger for canary and stable releases:
  # - Stable prod release: When PR from changeset-release/* branch is merged with [release] prefix
  # - Canary release: Push to dev or release/* branches (not from changeset-release merge)
  push:
    branches:
      - dev
      - 'release/**'
    paths:
      - '**'

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      YARN_ENABLE_IMMUTABLE_INSTALLS: false
    steps:
      - name: Generate GitHub App Token
        id: generate-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.GH_APP_ID }}
          private-key: ${{ secrets.GH_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}

      - name: Configure action
        id: configure-action
        run: |
          # Check commit message for [release] prefix to determine release type
          # For push events, head_commit.message contains the commit message
          COMMIT_MSG="${{ github.event.head_commit.message }}"
          if [[ "$COMMIT_MSG" =~ ^\[release\] ]]; then
            echo "isCanaryRelease=false" >> $GITHUB_OUTPUT
          else
            echo "isCanaryRelease=true" >> $GITHUB_OUTPUT
          fi

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          fetch-depth: 0
          persist-credentials: false
          token: ${{ steps.generate-token.outputs.token }}

      - name: Set git configuration
        run: |
          git config --global user.email builds@sitecore.com
          git config --global user.name "content-sdk-bot"
          git remote set-url origin https://x-access-token:${{ steps.generate-token.outputs.token }}@github.com/${{ github.repository }}

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: |
          yarn cache clean --all
          yarn install --immutable

      - name: Creating .npmrc
        run: |
          cat << EOF > "$HOME/.npmrc"
            @sitecore-content-sdk:registry=https://$NPM_REGISTRY
            registry=https://$NPM_REGISTRY
            //$NPM_REGISTRY/:_authToken=$NPM_TOKEN
          EOF
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_REGISTRY: ${{ secrets.NPM_REGISTRY }}

      - name: Pre-release version update (Canary)
        if: steps.configure-action.outputs.isCanaryRelease == 'true'
        run: |
          BRANCH_NAME="${{ github.ref_name }}"
          if [[ "$BRANCH_NAME" =~ ^release/ ]]; then
            TIMESTAMP=$(date -u +%Y%m%d%H%M%S)
            # For patch canaries: use timestamp to avoid potential canary version conflicts
            PREID="canary.${TIMESTAMP}"
          else
            PREID="canary"
          fi
          echo "Creating canary release on dev branch (no tags)..."
          npx lerna version prerelease --exact --preid "$PREID" --sign-git-tag false --yes --no-git-tag-version
          yarn install
          if [ -n "$(git status --porcelain)" ]; then
            git commit -a -m "chore: bump versions for canary release [skip ci]"
            git push origin HEAD:$BRANCH_NAME
          else
            echo "No version changes to commit"
          fi
        env:
          GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}

      - name: Pre-release canary publish
        if: steps.configure-action.outputs.isCanaryRelease == 'true'
        run: |
          BRANCH_NAME="${{ github.ref_name }}"
          if [[ "$BRANCH_NAME" =~ ^release/ ]]; then
            NPM_TAG="hotfix.rc.canary"
          else
            NPM_TAG="canary"
          fi
          echo "Publishing canary packages to npm..."
          npx lerna publish from-package --canary --pre-dist-tag "$NPM_TAG" --preid canary --no-verify-access --yes
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_REGISTRY: ${{ secrets.NPM_REGISTRY }}

      # ========== CHANGESET ACTION ==========
      # Create/update the release PR with updated versions based on changesets
      - name: Create Release Pull Request
        id: changesets
        uses: changesets/action@v1
        with:
          version: yarn changeset:cascade-version && node scripts/apply-caret-version-on-release.js && yarn install
          commit: '[release][ci] changelog, release and version bump'
          publish: changeset tag && lerna publish from-package --yes && git push --follow-tags
          title: ${{ github.ref_name == 'dev' && '[csdk] latest release' || format('[csdk] patch release - {0}', github.ref_name) }}
        env:
          GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}
